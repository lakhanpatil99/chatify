# Backend URL (no trailing slash). Required for production (Vercel + Render).
# Development: not needed (uses http://localhost:3000).
# Production: set in Vercel Environment Variables, e.g. https://your-backend.onrender.com
VITE_BACKEND_URL=

# Legacy; use VITE_BACKEND_URL if possible
# VITE_API_URL=
